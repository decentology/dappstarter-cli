export type DevelopConfig = {
	deviceId: string;
	projectUrl: string;
	remoteApiKey: string;
	remoteDeviceId: string;
	apiKey: string;
	port: number;
	syncPort: number;
	privateKey: string;
	publicKey: string;
};
